/**
 * Represents a DELETE request to the /api/word endpoint.
 * 
 * This is used to delete a single word.
 */
export default interface IDeleteWordRequest{

    // The word to delete
    word: string;

}